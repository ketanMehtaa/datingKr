/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['pbs.twimg.com', 'ph-avatars.imgix.net','lh3.googleusercontent.com','photosnow.org'],
  },
};

export default nextConfig;
